import { TechnologyPipePipe } from './technology-pipe.pipe';

describe('TechnologyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TechnologyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
